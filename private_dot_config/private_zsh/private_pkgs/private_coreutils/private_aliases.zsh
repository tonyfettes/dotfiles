# Add "safety net" for basic but irreversible file system operations by using
# verbose and interactive command modes. 
alias cp='command cp -vi'
alias mv='command mv -vi'
alias rm='command rm -vi'

# Make zsh more vim-like
alias :q='exit'
